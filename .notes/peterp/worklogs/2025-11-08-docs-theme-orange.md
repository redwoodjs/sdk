Investigation: opened `docs/src/styles/custom.css` to inspect existing accent variables. Found `--sl-color-text-accent` set to teal `#1ccabf`.
Decision: change the accent color variables to orange. Selected `#ff7a00` as the primary value.
Result: plan to update the CSS to use the new hex value so sidebar highlights render in orange.
