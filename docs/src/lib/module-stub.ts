// Empty stub for unused optional peer dependencies (e.g. flexsearch,
// @takumi-rs/image-response) that fumadocs packages declare but we don't use.
// Without this, rwsdk's client barrel throws "Could not resolve" errors.
export default {};
export const ImageResponse = undefined;
