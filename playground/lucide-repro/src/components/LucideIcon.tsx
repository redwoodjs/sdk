"use client";

// Import directly from lucide-react/Icon (barrel with "use client")
import Icon from "lucide-react/Icon";

export const LucideIcon = () => {
  return (
    <div>
      <Icon iconNode={[]} size={24} />
    </div>
  );
};
